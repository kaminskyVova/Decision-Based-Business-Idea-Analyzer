<!--
# Логика запроса недостающих данных 

Clarification Layer является вспомогательным механизмом Gatekeeper и:
- активируется только при `RETURN WITH CONDITIONS`;
- запрашивает строго недостающие данные;
- не выполняет анализ и не принимает решений.

После сбора данных управление всегда возвращается в Gatekeeper.
-->