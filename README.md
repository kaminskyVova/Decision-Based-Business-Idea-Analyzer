
Decision-Based Business Idea Analyzer
Система строгой оценки и допуска бизнес-идей.

Описание проекта

Decision-Based Business Idea Analyzer — это многослойная аналитическая система для строгой проверки бизнес-идей.
Проект предназначен для оценки целесообразности запуска инициатив на основе фактов, ограничений и ресурсов, а не интуиции или предположений.

Система ориентирована на снижение риска принятия необоснованных решений на ранней стадии.

Система не генерирует идеи и не принимает решения за пользователя.
Её задача — проверка возможности, выявление ограничений и формирование обоснованного аналитического вердикта.

Назначение системы

Проект решает ключевую проблему современных AI-ассистентов и агентных систем — отсутствие формального допуска к анализу.

В большинстве решений:

анализ запускается без достаточных данных;
система “догадывается” вместо проверки;
отсутствует ответственность за итоговое решение.
Данный проект вводит обязательный контур допуска (Gatekeeper) и запускает анализ только после прохождения всех проверок.

Архитектура (высокоуровневая)

Система состоит из трёх ключевых блоков:
- Gatekeeper (Ворота)
- Analysis (Анализ)
- Plan (План действий)
Порядок выполнения фиксирован и не нарушается.
Идея → Gatekeeper → Analysis → Decision → Plan (опционально)


1. Gatekeeper (ядро системы)
Gatekeeper — это обязательный первый этап обработки любой идеи.

Задачи Gatekeeper:
определить, готова ли идея к анализу;
проверить рамки и ответственность;
выявить критические ограничения;
принять процессное решение.
Gatekeeper не анализирует рынок, не считает экономику и не предлагает решений.

Проверки Gatekeeper:
Определение проблемы
Цель
Ограничения (регион — для офлайн-проектов город обязателен, капитал, сроки)
Ответственность за принятие решений
Проверка на реальность
Законность
Ресурсность (для производственных кейсов)

Возможные исходы:

HARD FAIL — анализ невозможен
RETURN WITH CONDITIONS — требуется уточнение данных
ADMITTED — допуск к анализу
Слой уточнения данных (Clarification Layer)
Clarification Layer — вспомогательный сервис Gatekeeper.
Он активируется только в случае RETURN WITH CONDITIONS и:
запрашивает строго недостающие данные;
не принимает решений;
возвращает управление обратно Gatekeeper.

Отказ на уровне Gatekeeper является корректным и ожидаемым результатом работы системы.

2. Analysis (многослойный анализ)
Анализ запускается только при статусе ADMITTED.

Назначение:
оценка идеи по нескольким независимым направлениям;
работа исключительно с подтверждёнными данными;
использование внешних источников и специализированных ИИ-модулей.
Примеры аналитических слоёв:
валидация идеи;
рынок и конкуренция;
финансовые и обязательные расходы;
ресурсы и реализуемость.

Аналитические слои не принимают решений самостоятельно и формируют вклад в итоговый обоснованый аналитический вердикт(не являющейся рекомендацией или решением) который формируется на уровне системы, а не отдельного аналитического слоя.
Итоговый статус является процессным результатом системы,
а не указанием к действию.
Итоговый статус является процессным результатом системы,
а не указанием к действию.
Анализ не допускает предположений при нехватке данных.
При отсутствии информации процесс останавливается.

3. Plan (план действий)
План формируется только если результат анализа положительный
или положительный при условиях.

План включает:
этапы реализации;
цели этапов;
контрольные точки;
ключевые риски.
Если итоговый вердикт — «нецелесообразно», план не создаётся.
Принципы проектирования
Проект следует подходу constraint-first design:
сначала ограничения и допуск;
затем анализ;
и только после этого действия.

Система допускает и фиксирует отказ как корректный результат.

Ограничения проекта

Проект не занимается:
мотивацией и коучингом;
управлением персоналом;
принятием решений за пользователя;
генерацией бизнес-идей;
субъективной оценкой без подтверждённых данных;
Проект работает строго в рамках закона.


Целевая аудитория

продуктовые и системные архитекторы;
AI / GenAI инженеры (orchestration, decision systems);
аналитические и инвестиционные команды;
предприниматели и стартапы (как прикладной сценарий использования);
работодатели, оценивающие архитектурное мышление.

Статус проекта

Проект находится в стадии архитектурной реализации и формализации.
Текущая версия предназначена для демонстрации:
системного мышления;
проектирования сложных AI-контуров;
работы с ограничениями и ответственностью.
Ключевой принцип
Если идея не проходит Gatekeeper — анализ не начинается.

Конец README.md
-----------------------------------------------------------------

Clarification Assistant — UI-Compatibility Mode (временный)
Проблема, которую решаем
Форма сейчас содержит поля:
Проблема
Цель
Регион
Капитал
Сроки
Ответственность
Но не содержит поля «Идея / объект оценки» и не различает тип запроса.
Из-за этого пользователь:
пишет идею в «Цель»;
пишет намерение в «Проблема»;
не понимает, что от него хотят.
Временное правило интерпретации (ВАЖНО)
Clarification Assistant НЕ делает выводов, но может классифицировать формулировки внутри проверки, чтобы не стопорить пользователя из-за UI.
Вводится правило совместимости:
1️⃣ Поле «Цель» в форме
Если в поле Цель указана формулировка вида:
«хочу открыть …»
«открыть …»
«запустить …»
👉 Clarification Assistant трактует это как ИДЕЮ (object of evaluation),
а не как цель проверки.
И:
не отклоняет ввод,
фиксирует отсутствие цели проверки,
запрашивает её отдельным вопросом.
2️⃣ Поле «Проблема» в форме
Если:
в «Проблеме» указано намерение, идея или пустота;
и нет явных признаков PROBLEM_SOLVING;
👉 Clarification Assistant:
не требует реальную проблему;
трактует сценарий как OPPORTUNITY;
запрашивает контекст / обоснование идеи.
3️⃣ Авто-режим запроса (временно)
Если пользователь не указал тип запроса явно:
по умолчанию считать OPPORTUNITY,
если нет явных описаний реального сбоя.
Это снимает 80% трения.
Как меняется поведение ассистента (пример)
Ввод пользователя (как сейчас):
Проблема: —
Цель: «Хочу открыть химчистку»
РАНЬШЕ (жёстко)
Цель некорректна. Уточните цель.
ТЕПЕРЬ (UI-compat)
Зафиксирована бизнес-идея: «открыть химчистку».
Уточните цель проверки (например: оценка целесообразности / рентабельности).
Без оценки. Без советов. Просто навигация.
Временный патч для SYSTEM PROMPT
Добавь в system prompt ОДИН блок (в конце):
Копировать код
Text
Временное правило совместимости с UI:

Если поле "Цель" содержит формулировку намерения
(«хочу открыть», «открыть», «запустить»),
ты должен трактовать её как описание бизнес-идеи,
а не как цель проверки.

В этом случае:
— не отклоняй ввод;
— зафиксируй отсутствие цели проверки;
— запроси формулировку цели проверки отдельно.

Если поле "Проблема" пусто или содержит намерение,
и нет признаков решения существующей проблемы,
ты должен трактовать запрос как OPPORTUNITY
и запросить контекст / обоснование идеи.